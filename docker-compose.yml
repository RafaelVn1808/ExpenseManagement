services:
  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: expense-db
    env_file:
      - .env
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_PID: "Express"
      SA_PASSWORD: ${MSSQL_SA_PASSWORD}
    ports:
      - "1433:1433"
    volumes:
      - mssql-data:/var/opt/mssql

  expense-api:
    build:
      context: .
      dockerfile: ExpenseManagement/Dockerfile
    container_name: expense-api
    env_file:
      - .env
    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Development}
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__DefaultConnection: ${DEFAULT_CONNECTION}
      Jwt__Key: ${JWT_KEY}
      Jwt__Issuer: ExpenseApi
      Jwt__Audience: ExpenseWeb
      PublicBaseUrl: ${PUBLIC_BASE_URL:-http://localhost:8080}
    depends_on:
      - db
    ports:
      - "8080:8080"
    volumes:
      - api-uploads:/app/wwwroot/uploads
      - data-protection-keys:/root/.aspnet/DataProtection-Keys

  expense-web:
    build:
      context: .
      dockerfile: ExpenseWeb/Dockerfile
    container_name: expense-web
    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Development}
      ASPNETCORE_URLS: http://+:8081
      ServiceUri__ExpenseApi: ${SERVICE_URI_EXPENSE_API:-http://expense-api:8080}
      Jwt__Key: ${JWT_KEY}
      Jwt__Issuer: ExpenseApi
      Jwt__Audience: ExpenseWeb
    depends_on:
      - expense-api
    ports:
      - "8081:8081"
    volumes:
      - data-protection-keys:/root/.aspnet/DataProtection-Keys

volumes:
  mssql-data:
  api-uploads:
  data-protection-keys:
